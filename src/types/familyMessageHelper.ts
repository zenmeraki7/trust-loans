export type FamilyMessageHelperData = {
  defaults: {
    contactName: string;
    appName: string;
    senderName: string;
    includeOfficialChannelsLine: boolean;
  };
  cautionPoints: string[];
  template: string;
};
